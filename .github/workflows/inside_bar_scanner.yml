name: 15-Min Inside-Bar Scanner

on:
  schedule:
    # Refresh the Nifty 50 universe at 09:10 IST (03:40 UTC), Mon-Fri.
    - cron: "40 3 * * 1-5"
    # Runs every 15 min from 10:30 to 14:30 IST, Mon-Fri.
    # GitHub Actions cron is in UTC. IST = UTC+5:30, so 10:30-14:30 IST = 05:00-09:00 UTC.
    - cron: "0,15,30,45 5 * * 1-5"
    - cron: "0,15,30 6 * * 1-5"
    - cron: "0 7 * * 1-5"
    - cron: "0,15,30,45 7 * * 1-5"
    - cron: "0,15,30,45 8 * * 1-5"
    - cron: "0 9 * * 1-5"
  workflow_dispatch: {}  # allows manual trigger for testing

jobs:
  refresh-universe:
    if: github.event_name == 'schedule' && github.event.schedule == '40 3 * * 1-5'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install yfinance requests python-dotenv

      - name: Restore latest Nifty 50 universe
        uses: actions/cache@v5
        with:
          path: nifty50_symbols.json
          key: nifty50-universe-${{ github.run_id }}
          restore-keys: |
            nifty50-universe-

      - name: Refresh Nifty 50 universe
        run: python scanner.py --refresh-universe

      - name: Save refreshed Nifty 50 universe
        uses: actions/cache/save@v5
        with:
          path: nifty50_symbols.json
          key: nifty50-universe-${{ github.run_id }}

  scan:
    if: github.event_name == 'workflow_dispatch' || github.event.schedule != '40 3 * * 1-5'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install yfinance requests python-dotenv

      - name: Restore today's alert state
        uses: actions/cache@v5
        with:
          path: alerted_today.json
          key: alerted-state-${{ github.run_id }}
          restore-keys: |
            alerted-state-

      - name: Restore latest Nifty 50 universe
        uses: actions/cache@v5
        with:
          path: nifty50_symbols.json
          key: nifty50-universe-${{ github.run_id }}
          restore-keys: |
            nifty50-universe-

      - name: Run scanner
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python scanner.py

      - name: Save today's alert state
        uses: actions/cache/save@v5
        if: always()
        with:
          path: alerted_today.json
          key: alerted-state-${{ github.run_id }}
